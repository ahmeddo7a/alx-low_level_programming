#include "main.h"
#include <stdio.h>

/**
 * add - add two numbers
 * @x: first number
 * @y: second number
 * Return: addition result
 */

int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
