#include "main.h"

/**
 * mul - multiply two numbers
 * @a: first number
 * @b: second number
 *
 * Return: On success mul value.
 * On error, 1
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
